# Security

High-level posture:

- Environment validation at boot
- JWT-based authentication with rotation support
- RBAC (role + permission model)
- Rate limiting
- Structured logging
- Deterministic signal execution (no hidden ML drift)

Implementation details intentionally omitted from public repo.
